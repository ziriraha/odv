# Odoo Dev Tool (odv)
All in one tool for Odoo development.

## Install Guide
To install, run: `go install`. Make sure to have go installed locally.
First set the $ODOO_HOME env variable to the parent directory of where your community, enterprise and upgrade repos are.
