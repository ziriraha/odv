# odoodev
All in one tool for Odoo development.

## TODO
- [ ] Get branch names from a config file (to also checkout to specific commits instead of version branches)

## Install Guide
To install run: `go install`. Make sure to have go installed locally.
First set the $ODOO_HOME env variable to the path of where your community, enterprise and upgrade repos are.
